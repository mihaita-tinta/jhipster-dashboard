spring:
    application:
        name: dashboard
    cloud:
        config:
            fail-fast: true
            uri: http://localhost:8761/config
            label: master
            name: dashboard
            profile: prod
